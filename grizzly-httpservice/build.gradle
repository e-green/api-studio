apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'osgi'

//group = 'org.glassfish.grizzly.osgi'
//version = '2.3.26-SNAPSHOT'
//
//description = """Grizzly OSGi HttpService"""


jar {
    manifest {
        attributes(
                "Bundle-Activator": "org.glassfish.grizzly.osgi.httpservice.Activator")
    }
}


dependencies {
    compile group: 'javax.servlet', name: 'javax.servlet-api', version: '3.1.0'
    compile group: 'org.glassfish.grizzly', name: 'grizzly-http-servlet', version: '2.3.26-SNAPSHOT'
    compile group: 'org.glassfish.grizzly', name: 'grizzly-http', version: '2.3.26-SNAPSHOT'
    compile group: 'org.glassfish.grizzly', name: 'grizzly-framework', version: '2.3.26-SNAPSHOT'
    compile group: 'org.glassfish.grizzly', name: 'grizzly-portunif', version: '2.3.26-SNAPSHOT'
    compile group: 'org.glassfish.grizzly', name: 'grizzly-comet', version: '2.3.26-SNAPSHOT'
    compile group: 'org.glassfish.grizzly', name: 'grizzly-websockets', version: '2.3.26-SNAPSHOT'
    testCompile group: 'junit', name: 'junit', version: '4.7'
    compile(group: 'org.osgi', name: 'org.osgi.core', version: '4.2.0') {
        /* This dependency was originally in the Maven provided scope, but the project was not of type war.
        This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
        Please review and delete this closure when resolved. */
    }
    compile(group: 'org.osgi', name: 'org.osgi.compendium', version: '4.2.0') {
        /* This dependency was originally in the Maven provided scope, but the project was not of type war.
        This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
        Please review and delete this closure when resolved. */
    }
}
